#include <bits/stdc++.h>
using namespace std;

int main() {
    int vertices, edges, a, b;
    cin >> vertices >> edges;
    
    for (int i = 0; i < edges; ++i) {
        cin >> a >> b;
    }
    return 0;
}